# Dependencies
node_modules/

# Framework build output and local caches
.next/
.next-local/
.next-local-*/
.npm-cache/
work/npm-cache/
dist/

# Local tooling output
*.log
*.tsbuildinfo

# Local secrets — never commit these files. The example is safe to share.
.env*
!.env.example

# Runtime reports are created locally and may contain internal server addresses.
data/it-support-reports.json
